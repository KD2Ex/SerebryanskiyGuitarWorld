<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginRight="10sp"
    >

    <ImageView
        android:id="@+id/homeCategoryImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/guitar_category"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/homeCategoryTitle"
        android:text="Гитары"

        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="@+id/homeCategoryImage"
        app:layout_constraintEnd_toEndOf="@+id/homeCategoryImage"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginBottom="5sp"/>

</androidx.constraintlayout.widget.ConstraintLayout>